{
  "name": "velorate",
  "private": true,
  "version": "0.0.0",
  "description": "Fast, flexible and production-ready rate limiting for modern applications.",
  "license": "MIT",
  "type": "module",

  "packageManager": "pnpm@11.10.0",

  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "typecheck": "turbo run typecheck"
  },

  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.10.0",
      "onFail": "download"
    }
  },

  "devDependencies": {
    "turbo": "^2.10.4"
  }
}